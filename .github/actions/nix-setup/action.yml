name: Set up Nix
description: >-
  Install Nix (Determinate Systems installer) and, when a cache name is
  provided, configure the Cachix binary cache. Pushes to the cache only on
  non-pull_request events that carry an auth token; every other run, including
  fork pull requests, is pull-only. Shared by the CI, docs, release, and
  flake-update workflows so the installer and Cachix wiring lives in exactly
  one place.

# Checkout remains in the caller because this local action is unavailable before checkout.

inputs:
  cachix-cache:
    description: Cachix cache name. When empty, Cachix is not configured at all.
    required: false
    default: ""
  cachix-auth-token:
    description: >-
      Cachix auth token (pass the CACHIX_AUTH_TOKEN repository secret). When
      empty — or on pull_request events — the cache is configured pull-only so
      untrusted runs can never push. A contract test asserts that this file
      never names a repository secret at all, so the only place a token can be
      wired in is the calling workflow.
    required: false
    default: ""

runs:
  using: composite
  steps:
    - uses: DeterminateSystems/nix-installer-action@3138316df39ed29be04236d7ffc686fa525866aa # v23

    # Pull requests must never receive a token that can write to the cache.
    - name: Resolve Cachix mode
      id: cachix-mode
      shell: bash
      env:
        CACHIX_AUTH_TOKEN: ${{ inputs.cachix-auth-token }}
        CACHIX_CACHE: ${{ inputs.cachix-cache }}
        EVENT_NAME: ${{ github.event_name }}
      run: |
        set -euo pipefail
        push=false
        if [[ -n "$CACHIX_AUTH_TOKEN" && "$EVENT_NAME" != "pull_request" ]]; then
          push=true
        fi
        printf 'push=%s\n' "$push" >> "$GITHUB_OUTPUT"
        if [[ -z "$CACHIX_CACHE" ]]; then
          echo "::warning::CACHIX_CACHE is unset; building without a binary cache"
        fi

    - name: Configure Cachix (pull-only)
      if: ${{ inputs.cachix-cache != '' && steps.cachix-mode.outputs.push != 'true' }}
      uses: cachix/cachix-action@38b082610b782e7e93e209c35fd730d399dee866 # v17
      with:
        name: ${{ inputs.cachix-cache }}
        extraPullNames: nix-community
        skipPush: true

    - name: Configure Cachix (push-enabled)
      if: ${{ inputs.cachix-cache != '' && steps.cachix-mode.outputs.push == 'true' }}
      uses: cachix/cachix-action@38b082610b782e7e93e209c35fd730d399dee866 # v17
      with:
        name: ${{ inputs.cachix-cache }}
        authToken: ${{ inputs.cachix-auth-token }}
        extraPullNames: nix-community
