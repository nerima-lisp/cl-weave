# Compiled Lisp artifacts
*.fasl
*.abcl
*.lx64fsl
*.dx64fsl

# Coverage and test run artifacts
coverage-report-*/
coverage-filter-*/
cl-weave-coverage-report/
# Snapshot directory some self-tests write under the working tree when no
# temporary root env var is set; it is regenerated and must not be committed.
cl-weave-core-snapshots/
cl-weave.coverage
cl-weave-results.json
cl-weave-events.jsonl
cl-weave-cli-results.json
cl-weave-metadata.json
cl-weave-plan.json
cl-weave-watch-once.json
cl-weave-tap.txt
cl-weave-junit.xml
cl-weave-doctor.json

# Nix
result
result-*
.direnv/

# MkDocs local build output (docs/mkdocs.yml sets site_dir: ../site)
/site/

# Editor and OS noise
.DS_Store
*~
\#*\#
.\#*
tmp/
.worktrees/

# Claude Code tooling state (worktrees, session data)
.claude/
